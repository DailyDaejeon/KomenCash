// import jwtDecode from 'jwt-decode'

const state = {
};

const mutations = {
    
}

const actions = {
    

};

const getters = {
    
}

export default {
    state,
    mutations,
    getters,
    actions
}
