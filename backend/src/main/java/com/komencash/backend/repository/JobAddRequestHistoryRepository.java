package com.komencash.backend.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface JobAddRequestHistoryRepository extends JpaRepository<JobAddRequestHistoryRepository, Integer> {
}