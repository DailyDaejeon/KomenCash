server:
  port: 8081
  servlet:
    context-path: /api

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ^b203root
    url: jdbc:mysql://localhost:3306/komencash?characterEncoding=UTF-8&serverTimezone=UTC

  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
